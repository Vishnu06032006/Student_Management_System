// Centralized so Phase 8's SystemSetting module can later make this
// configurable without touching every place that reads it.
const ATTENDANCE_ELIGIBILITY_THRESHOLD = 75;

module.exports = { ATTENDANCE_ELIGIBILITY_THRESHOLD };
